# v0.5.6 complete — shell safety, Innertube, Deno/EJS, honest Chrome DPAPI

**Package version:** 0.5.6  
**HEAD:** this commit (`docs: v0.5.6 complete`)  
**Suite:** `python -m pytest -q` → **382 passed / 0 skipped / 0 failed** (confirm on this commit)  
**Baseline:** `c836df8` (v0.5.4, 349 passed)

Primary GUI remains **Flet 0.86.5**. Sequential worker. Enqueue does not auto-start. Clear never deletes media. No concurrent downloads. No bot bypass. **No Windows shell / Explorer theme mutation.**

## Required table

| Item | Status | Evidence |
|------|--------|----------|
| GetForegroundWindow removed from DWM path | **Fixed** | `disable_dwm_glass` is a no-op; `apply_page_chrome` is Flet-only. `test_shell_safety.py` |
| No system theme/Explorer mutation | **Fixed** | `.cursor/rules/windows-shell-safety.mdc`; [SHELL_SAFETY.md](SHELL_SAFETY.md). Reveal file still uses `explorer /select,` only. |
| windows-shell-safety.mdc present | **Fixed** | `.cursor/rules/windows-shell-safety.mdc` (`alwaysApply`) |
| player_client rotation | **Fixed** | `--extractor-args youtube:player_client=android_vr,tv_downgraded,web_embedded,web_safari`. [YOUTUBE_CLIENTS.md](YOUTUBE_CLIENTS.md), `test_youtube_clients.py` |
| Deno/EJS in worker | **Fixed** | PATH inherited; `--js-runtimes deno:<path>` or `node:<path>`. `test_js_runtime_ejs.py` |
| js_runtime classification | **Fixed** | n-challenge / only images / EJS wiki → `js_runtime`, not unknown, not Re-authenticate |
| Auth UX honest | **Fixed** | Firefox + cookies.txt first; Chrome ABE copyable limit. [COOKIES.md](COOKIES.md) |
| Inter-job delay | **Fixed** | Default 3s (`inter_job_delay_sec` 0–60). [SPEED.md](SPEED.md), `test_inter_job_delay.py` |

## Also in this pass (v0.5.5 leftovers)

| Item | Status | Evidence |
|------|--------|----------|
| Quit / destroy awaited | **Fixed** | `a8e5d1d`; `test_ui_flet_shutdown.py` |
| Cancel while Starting | **Fixed** | `8b06b49`; `test_cancel_while_starting.py` |
| Thumbnails | **Fixed** | `--write-thumbnail` + sidecar/backfill; `test_completed_thumbnails.py` |
| ffmpeg discovery | **Fixed** | PATH + WinGet Gyan; `test_ffmpeg_discovery.py` |
| Copy error works | **Fixed** | Flet `Clipboard.set` (async), not `page.set_clipboard`; `test_error_report.py` |
| Download selected visible | **Fixed** | Floating bar above the queue list when pending is selected |
| Chrome DPAPI | **Honest limit** | Not bypassed. Error text says FrameForge cannot fix ABE. |
| Drag | **Partial** | Unchanged: custom `WindowDragArea`. Not claimed fixed. |

## Residual

- Drag: eyes-on still required on Win11 + 680M.
- YouTube may throttle below NIC speed even with EJS + Innertube.
- Age/members/bot wall still need Firefox cookies or cookies.txt.
- Packaged `dist\FrameForge\FrameForge.exe` is stale until `.\scripts\build_portable.ps1` (portable builds should note Deno on PATH).

## Commits (this pass)

1. `fix: await window destroy and hard force quit`
2. `fix: cancel while starting`
3. `fix: thumbnails for completed downloads`
4. `feat: inter-job delay and fast default throughput`
5. `fix: deno/ejs path and youtube js challenge errors`
6. `fix: never apply DWM chrome to foreign HWNDs (Explorer safety)`
7. `fix: ffmpeg discovery from PATH and WinGet Gyan`
8. `fix: clipboard copy and download selected affordance`
9. `feat: youtube innertube player_client rotation for anonymous public downloads`
10. `fix: deno/ejs in worker and js_runtime error class`
11. `feat: auth paths prefer firefox/cookies.txt; honest chrome DPAPI limits`
12. `docs: v0.5.6 complete`
