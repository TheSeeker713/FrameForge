# v0.6.3 complete — field migrate, repair UX, download artifacts

**Package version:** 0.6.3

## Library Move (field)

Not claimed from unit tests alone. A **3-file cross-drive** Move (`D:`/`C:` → `K:\…\FrameForge\Library\Uncategorized`) finished with **moved=3**, failed=0. Log: [FIELD_MIGRATE_v0.6.3.md](FIELD_MIGRATE_v0.6.3.md).

Also:

- Per-file log under `Downloads\FrameForge\temp\library_move_<timestamp>.log`
- Drop stale `library_items` whose path is missing (after heal) so jobs can move again
- Skip `.part` / aria2 / zero-byte; post-move scan is Uncategorized only
- Import / Scan CTAs remain when onboarded but incomplete

## Repair folders

Button shows **Repairing folders…** (disabled while running), progress text, then a **summary dialog** (thumbs, junk → `temp/junk`, info.json → `metadata/`). Errors are visible. Never Recycles.

## Download layout

yt-dlp `-P home:<site>` / `-P temp:temp/dl`. Finished media in `youtube/` etc. `.info.json` → `metadata/` after success.

## Docs

[LIBRARY.md](LIBRARY.md), [FOLDER_LAYOUT.md](FOLDER_LAYOUT.md), [FIELD_MIGRATE_v0.6.3.md](FIELD_MIGRATE_v0.6.3.md).

## Suite

`python -m pytest -q` → **469 passed / 0 skipped / 0 failed** (2026-08-16).
