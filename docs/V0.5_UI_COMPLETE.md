# v0.5.0 complete — Flet GUI rewrite

**Package version:** 0.5.0  
**HEAD:** recorded at this commit (`docs: v0.5.0 Flet UI complete`)  
**Suite:** `python -m pytest -q` → **285 passed / 0 skipped / 0 failed**

Primary GUI is **Flet 0.86.5**. `python -m frameforge --gui` launches `frameforge.ui_flet.app.run_gui`. CustomTkinter is not mixed into that window. Sequential single-active media stage, enqueue-does-not-auto-start, site folders, pause/resume, quit policy, tray (pystray), cookies, playlists, convert, shortcuts ids, history v2, queue clear, and fail-pause are preserved.

## Acceptance

| Check | Result |
|-------|--------|
| Settings single-instance | Pass (`test_settings_single_instance`) |
| Retry failed → fail again → disarm + fail-pause UI | Pass (`test_flet_retry_fail_again_increments_fail_pause`, `test_retry_fail_again_uses_same_fail_pause_handler`) |
| Empty selection → no Upscale/Convert | Pass (`floating_bar_view`) |
| Eligible selection → Upscale | Pass |
| Queue/History `ListView`, Thumbnails `GridView` | Pass |
| Enqueue never arms | Pass |
| Full suite 100% | **285 passed** |
| Manual checklist | [ACCEPTANCE_V05.md](ACCEPTANCE_V05.md) |

## Design

Locked light tokens in `frameforge.ui_flet.theme`. Cards, floating bar, pill tabs, Settings three cards. See [UI_REDESIGN.md](UI_REDESIGN.md).

## Fail-pause on retry

`UiBridge.retry_job` is the only Retry path (modal and failed card). Hard `unknown` (plus auth/bot) fail-pauses while armed. yt-dlp non-zero exits include a stderr tail so bot/auth text is classified.

## Tray / packaging

System tray remains **pystray** (Flet has no equivalent used here). PyInstaller path must be revalidated after this GUI rewrite (not in this pass).
