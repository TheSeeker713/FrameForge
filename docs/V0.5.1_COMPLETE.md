# v0.5.1 complete — Flet interaction emergency fix

**Package version:** 0.5.1  
**HEAD:** this commit (`docs: v0.5.1 Flet interaction fix complete`)  
**Suite:** `python -m pytest -q` → **298 passed / 0 skipped / 0 failed**  
**Baseline:** `532252f` (v0.5.0)

Primary GUI remains **Flet 0.86.5**. No visual redesign. `UiBridge` + sequential worker + enqueue-does-not-auto-start unchanged.

## User bugs

| # | Report | Fixed |
|---|--------|-------|
| 1 | Window drag creates a ghost/stale window | **Yes** — opaque `window.bgcolor`, `shadow=False`, native title bar. [UI_WINDOW_FIX.md](UI_WINDOW_FIX.md) |
| 2 | Queue with completed/failed had almost no actions unless multi-select | **Yes** — always-on chrome: Download all pending / Retry failed / Clear finished / Clear selected |
| 3 | **More** did nothing | **Yes** — `PopupMenuItem.on_click` (no nested button); every item calls `UiBridge` / existing actions |
| 4 | Import TXT/MD did nothing | **Yes** — FilePicker → `preview_import` → confirm → `confirm_add` pending only |
| 5 | Authenticate modal could not close; process stuck after window close | **Yes** — `DialogHost` pop_dialog / X / Esc / barrier; window CLOSE → `shutdown` + `os._exit(0)`. [UI_SHUTDOWN.md](UI_SHUTDOWN.md) |

## Acceptance

See [ACCEPTANCE_V051.md](ACCEPTANCE_V051.md) and [AUDIT_UI_V051.md](AUDIT_UI_V051.md).
