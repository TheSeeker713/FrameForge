# v0.5.3 complete — P0 field recovery

**Package version:** 0.5.3  
**HEAD:** this commit (`docs: v0.5.3 complete`)  
**Suite:** `python -m pytest -q` → **330 passed / 0 skipped / 0 failed**  
**Baseline:** `20a990d` (v0.5.2)

Primary GUI remains **Flet 0.86.5**. Sequential worker. Enqueue does not auto-start. Clear never deletes media.

## User reports

| # | Report | Status |
|---|--------|--------|
| 1 | Clear finished emptied the entire queue | **Fixed** |
| 2 | Window drag: outline only | **Partial** — v0.5.2 claim failed; v0.5.3 reapplies opaque chrome + disables Mica. Confirm on hardware. |
| 3 | Import dedupe of already-downloaded | **Keep** (not a bug) |
| 4 | Download all pending: no progress | **Fixed** |
| 5 | Fail-pause popup | **Keep** |
| 6 | Import from browser Firefox-only | **Fixed** (Chrome / Edge / Firefox) |
| 7 | Authenticate modal vanished | **Fixed** (stays open with status) |
| 8 | Failed jobs invisible unless selected | **Fixed** |
| 9 | Retry selected: no visual | **Fixed** |
| 10 | No download activity cue | **Fixed** |
| 11 | Close freezes the GUI | **Fixed** (watchdog + second-close force) — confirm on hardware |
| 12 | Unrecoverable stuck state | **Partial** — force-quit hatch; not every freeze mode can be proven in pytest |

## Residual limitation

Title-bar drag may still show an outline-only window on this Flet/Flutter + AMD iGPU stack; FrameForge now forces an opaque HWND on every move, but DWM can still fail. Rebuild `dist\FrameForge\FrameForge.exe` to pick up 0.5.3 (the v0.5.2 folder is stale).
