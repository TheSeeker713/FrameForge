# v0.5.4 complete — P0 field (quit, halt, parity, copy, drag)

**Package version:** 0.5.4  
**HEAD:** this commit (`docs: v0.5.4 complete`)  
**Suite:** `python -m pytest -q` → **349 passed / 0 skipped / 0 failed**  
**Baseline:** `be09f4a` (v0.5.3, 330 passed)

Primary GUI remains **Flet 0.86.5**. Sequential worker. Enqueue does not auto-start. Clear never deletes media. No concurrent downloads. No bot bypass.

## User reports

| # | Issue | Fixed / Partial / Failed | Evidence |
|---|--------|----------------------------|----------|
| 1 | Drag visibility | **Partial** | New mechanism: hidden native caption + Flutter `WindowDragArea` ([UI_WINDOW_FIX.md](UI_WINDOW_FIX.md)). Not another bgcolor pass. Eyes-on on Win11 + 680M still required. |
| 2 | Pause/Cancel while downloading | **Fixed** | Header and queue chrome Pause/Stop; `test_transport_fail_pause.py` |
| 3 | Fail-pause stops bulk | **Fixed** | `halt_after_fail` latch; job 2 not claimed; handler-failed-without-raise path |
| 4 | Unknown vs CLI parity | **Partial** | Sticky cookies, empty `--cookies`, missing aria2c, cwd, ffmpeg PATH, stderr/argv logging ([YTDLP_PARITY.md](YTDLP_PARITY.md)). Remaining: venv `python -m yt_dlp` vs PATH `yt-dlp`, `-f bv*+ba/b`, download-archive. |
| 5 | Copyable errors | **Fixed** | Copy full report on fail-pause, Authenticate, failed cards (`test_error_report.py`) |
| 6 | X always quits (force available) | **Fixed** | Always a quit dialog; Force quit + watchdog `_exit`; custom title-bar Close uses the same path (`test_ui_flet_shutdown.py`) |

## Residual

- Drag: if `WindowDragArea` still blanks on this Flet/Flutter + AMD iGPU stack, that is a compositor limit. Maximize/restore or Win+Arrow remain workarounds.
- CLI parity: copy the job’s `ytdlp_invocation` and run that argv from the recorded `cwd` to compare with a bare `yt-dlp URL`.
- Packaged `dist\FrameForge\FrameForge.exe` is only current after `.\scripts\build_portable.ps1`.

## Commits (this pass)

1. `fix: quit dialog always available with force quit`
2. `fix: pause/stop while armed and fail-pause halts bulk`
3. `fix: yt-dlp invocation parity with CLI`
4. `feat: copy full error report`
5. `fix: window content visible during drag (escalation)`
6. `docs: v0.5.4 complete`
